# following xgcm ci environment specs
# see: https://pip.pypa.io/en/stable/reference/pip_install/#git
# for how to specify branches of repos
name: py37_yml
channels:
  - conda-forge
dependencies:
    - python=3.7
    - future
    - xarray
    - netCDF4
    - bottleneck
    - dask
    - numpy
    - scipy
    - pytest-cov 
    - docrep
    - pyresample
    - cmocean
    - datashader
    - holoviews
    - geoviews
# Dependencies for xmitgcm, xgcm 
# Not obtained if cloning those repos from git
    - cachetools
      #    - fsspec
      #    - zarr
      #    - cftime
